---
# Standards: 0.2
dependency:
  name: galaxy
driver:
  name: docker
lint: |
  set -e
  yamllint .
  ansible-lint
platforms:
  - name: base-common-centos7
    image: milcom/centos7-systemd
    privileged: true
  - name: base-common-centos8
    image: centos
    privileged: true
provisioner:
  name: ansible
verifier:
  name: ansible
